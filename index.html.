<!doctype html>
<html lang="vi">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>iOSViệt.Co — Mockup</title>

  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@300;600;800&family=Roboto:wght@300;500;700&display=swap" rel="stylesheet">

  <style>
    :root{
      --bg: #f5f6f7;
      --card: #ffffff;
      --accent: #37b27c; /* green banner */
      --header: #1b2b3a; /* dark header */
      --muted: #8a8f98;
      --danger: #e04f4f;
      --vip: #c94242;
      --radius: 12px;
      font-family: "Nunito", "Roboto", sans-serif;
    }

    html,body { height:100%; margin:0; background:var(--bg); color:#333; -webkit-font-smoothing:antialiased; -moz-osx-font-smoothing:grayscale; }
    /* Top bar like mobile status + header */
    .top-header {
      background: linear-gradient(180deg, rgba(0,0,0,0.06), rgba(0,0,0,0.02));
      box-shadow: 0 1px 0 rgba(0,0,0,0.06);
      position: sticky;
      top:0;
      z-index:100;
    }
    .top-header .nav {
      max-width:640px;
      margin:0 auto;
      display:flex;
      align-items:center;
      justify-content:space-between;
      padding:12px 16px;
    }
    .brand {
      display:flex;
      gap:12px;
      align-items:center;
    }
    .logo {
      width:38px; height:38px; border-radius:6px;
      background:linear-gradient(135deg,#3e6fb0,#4ec5c5);
      display:flex; align-items:center; justify-content:center;
      box-shadow: 0 2px 6px rgba(0,0,0,0.12);
      color:white; font-weight:800; font-size:14px;
    }
    .brand h1{
      margin:0; color:#5fb0ff; font-size:18px; letter-spacing:0.4px;
    }
    .hamburger {
      width:44px; height:44px; display:flex; align-items:center; justify-content:center;
      border-radius:10px; background:transparent; border:0; cursor:pointer;
    }
    .hamburger svg { opacity:0.95; }

    .container { max-width:640px; margin:18px auto; padding:0 14px 80px; }

    /* hero card */
    .hero {
      background:var(--accent);
      border-radius:10px;
      color:#fff;
      padding:18px;
      display:flex;
      gap:14px;
      align-items:center;
      box-shadow: 0 8px 30px rgba(37,168,134,0.08);
    }
    .hero .avatar {
      width:64px; height:64px; border-radius:12px; background:#0f1714;
      display:flex; align-items:center; justify-content:center;
      box-shadow: 0 6px 18px rgba(0,0,0,0.15);
    }
    .hero h2 { margin:0; font-size:20px; font-weight:800; letter-spacing:0.6px; }
    .hero p { margin:4px 0 0 0; font-size:13px; opacity:0.95; }

    /* section title */ 
    .join-telegram {
      text-align:center; color:#cf4541; margin:22px 0; font-weight:700; font-size:18px;
    }

    /* cards list */
    .card {
      background:var(--card);
      border-radius:12px;
      padding:18px;
      box-shadow: 0 2px 6px rgba(18,24,30,0.04);
      margin-bottom:18px;
      display:flex;
      align-items:center;
      justify-content:space-between;
    }
    .card .left {
      display:flex; gap:12px; align-items:center;
    }
    .badge {
      width:44px; height:44px; border-radius:10px;
      display:flex; align-items:center; justify-content:center;
      font-weight:700; color:#fff;
    }
    .badge.vip { background: linear-gradient(180deg, #ff6b6b, #c94242); }
    .badge.free { background: #9aa0a7; }
    .card .title {
      font-weight:800; font-size:16px; color:#3d3d3d;
    }
    .card a.more { color:#2f6ddf; text-decoration:none; font-size:14px; }

    .footer {
      margin-top:28px; text-align:center; color: #7a7a7a; font-size:15px;
      padding:22px 0 80px;
    }

    /* bottom url bar mockup */
    .mobile-url {
      position: fixed;
      left:50%;
      transform: translateX(-50%);
      bottom:12px;
      width:92%;
      max-width:640px;
      background:rgba(255,255,255,0.95);
      border-radius:14px;
      box-shadow:0 12px 30px rgba(20,20,20,0.16);
      display:flex; align-items:center; gap:12px; padding:10px 12px;
      z-index:200;
    }
    .mobile-url .aa { font-weight:700; color:#666; padding:6px 8px; border-radius:8px; background:transparent; }
    .mobile-url .url { flex:1; text-align:center; color:#333; font-weight:600; }
    .mobile-url .icon { width:34px; height:34px; display:flex; align-items:center; justify-content:center; border-radius:8px; background:transparent; }

    /* responsive tweaks */
    @media (min-width:720px){
      body { background:#fff; }
      .container { margin-top:28px; }
      .mobile-url { display:none; }
    }
  </style>
</head>
<body>

  <heade
